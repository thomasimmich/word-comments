{
  "name": "word-annotations",
  "version": "1.0.0",
  "main": "dist/server.js",
  "scripts": {
    "start": "node dist/server.js",
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "tsc && tsc -p tsconfig.netlify.json",
    "build:main": "tsc",
    "build:netlify": "tsc -p tsconfig.netlify.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "A server for handling Word document operations",
  "dependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.24",
    "docx": "^8.5.0",
    "express": "^4.18.3",
    "mammoth": "^1.6.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "devDependencies": {
    "@netlify/functions": "^3.1.9",
    "@types/aws-lambda": "^8.10.149",
    "nodemon": "^3.1.10",
    "ts-node-dev": "^2.0.0"
  }
}
